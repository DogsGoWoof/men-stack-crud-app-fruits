<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <title>Edit</title>
</head>

<body>
    <!-- update the header -->
    <h1>Edit <%= fruit.name %>
    </h1>
    <form action="/fruits/<%=fruit._id%>?_method=PUT" method="POST">
        <label for="name">Name:</label>
        <input type="text" name="name" value="<%= fruit.name %>" />

        <div class="checkbox-div">
            <label for="ready-to-eat">Ready to Eat?</label>
            <% if (fruit.isReadyToEat) { %>
                <input type="checkbox" name="isReadyToEat" id="ready-to-eat" checked />
            <% } else { %>
                <input type="checkbox" name="isReadyToEat" id="ready-to-eat" />
            <% } %>
        </div>
        <label for="emoji">Emoji Unicode:</label>
        <div>
        <input type="text" name="emoji" value="<%= fruit.emoji %>" style="display: inline;" />
        <% if (fruit.emoji) { %>
            <div class="emoji-bg">
                <p class="emoji">&#x<%= fruit.emoji %></p>
            </div>
        <% } else { %>
            <div class="emoji-bg">
                <p class="emoji">&#x1F9FA</p>
            </div>
        <% } %>
        </div>
        <!-- update the button text -->
        <button type="submit">Update Fruit</button>
    </form>
    <a class="fruits-index-link" href="/fruits">
        <div class="index-link-text">Back to Fruits</div>
    </a>
</body>

</html>